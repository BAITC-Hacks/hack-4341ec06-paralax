# Задачи команды на 5 часов

Точка отсчёта `T0` — фактическое начало разработки. Вписать реальные имена ниже: **участник 1/UI — ___; участник 2/данные — ___; участник 3/API и интегратор — ___**. Если интегратором будет другой человек, исправить эту строку и сообщить всей команде. Трек выбран: автоматизация заказов поставщикам. Точное ТЗ — [`spec.md`](spec.md).

Каждый работает в своей ветке `feat/<имя>/<задача>` и отдельном клоне/worktree. Общие `contracts/` и `fixtures/` меняет участник 3. Все ссылки на PR/коммиты и фактическое время вписываются в эту страницу; задачи ниже **ещё не считаются выполненными**.

## Участник 1 — UI (`frontend/`)

| Срок | Задача | Результат для проверки | Статус |
| --- | --- | --- | --- |
| T+20 мин | Сверстать один рабочий экран по разделу 7 ТЗ: шапка, параметры, сводка, таблица, панель факторов. | Экран открывается на синтетической фикстуре; есть скриншот. | Запланировано |
| T+60 мин | Подключить валидный `PlanningResult`, сортировку по срочности, поиск/фильтр; состояния загрузки, пустого результата и ошибки. | `npm run check` проходит; можно выбрать SKU и увидеть факторы. | Запланировано |
| T+120 мин | Подключить реальный `POST /api/planning-runs` после готовности API. | Первый расчёт виден без ручной подмены JSON. | Запланировано |
| T+180 мин | Ручная правка выбранного количества, явное утверждение, скачивание CSV, кнопка AI-объяснения. | Демонстрация действий от расчёта до скачанного файла. | Запланировано |
| T+240 мин | Исправить UI-блокеры интеграции и проверить работу на ширине ноутбука. | Скриншот/короткое видео, зелёные frontend-проверки. | Запланировано |

## Участник 2 — Excel и расчёт (`backend/importers/`, `backend/planning.py`)

| Срок | Задача | Результат для проверки | Статус |
| --- | --- | --- | --- |
| T+20 мин | Сопоставить реальные колонки IEK с контрактом; указать даты срезов, код 1С, охват остатков и отсутствующие поля. | Карта полей в `docs/data-map.md`; решение о 3–5 SKU для демо. | Запланировано |
| T+60 мин | Импорт продаж, остатков и товара в пути для выбранных SKU; проверка дубликатов и единиц. | Нормализованный набор проходит схему; приватный Excel не попал в Git. | Запланировано |
| T+120 мин | Функция `plan(input)` с горизонтом, базовым спросом, запасом и округлением. | Тест формулы и монотонности товара в пути проходит. | Запланировано |
| T+180 мин | Флаг крупного документа, сезонность при достаточной истории, подтверждённый/предполагаемый дефицит. | Приёмочные тесты показывают флаги и влияние факторов; неизвестное отмечено как допущение. | Запланировано |
| T+240 мин | Проверить 20–50 SKU IEK или объяснить невозможность; исправить крайние случаи. | Выход соответствует `PlanningResult`, Python-проверки зелёные. | Запланировано |

## Участник 3 — контракты, API, AI, интеграция (`contracts/`, `fixtures/`, `backend/app.py`, CI)

| Срок | Задача | Результат для проверки | Статус |
| --- | --- | --- | --- |
| T+20 мин | Утвердить стек/допущения; выпустить входной контракт, совместимый с Excel, и валидные синтетические фикстуры. | Версия схемы и её тест в `main`; остальные обновили ветки. | Запланировано |
| T+60 мин | Проверить/довести `python scripts/check.py`, CI; API пока может возвращать фикстуру. | Локальный `check` и GitHub Actions выполняются. | Каркас подготовлен; проверить в CI |
| T+120 мин | Подключить `plan` к `POST /api/planning-runs`; валидация и понятные ошибки. | Запрос возвращает валидный `PlanningResult`. | Запланировано |
| T+180 мин | Реализовать ручные операции API и OpenAI-объяснение с проверкой полей/чисел и fallback. | Ошибка или отсутствие ключа не ломает расчёт и экспорт. | Запланировано |
| T+240 мин | Последовательно интегрировать проверенные PR, обновить README с командами и провести полный `check`. | Общий запуск и ссылки на коммиты трёх участников. | Запланировано |

## Общие контрольные точки

- Каждый час: отдельный фактический отчёт `docs/updates/<имя>/<HHMM>.md` с коммитом/PR, запущенными проверками, демо-артефактом и блокером.
- Перед PR: `git fetch origin`, `git rebase origin/main`, затем `python scripts/check.py`. Не мержить в `main` без подтверждения интегратора.
- T+240–270 мин: чистый запуск по README, проверка экспорта и работы без OpenAI API, устранение только блокеров.
- T+270–300 мин: финальный демо-прогон 60–90 секунд, питч, ссылки на личные коммиты и резервный скриншот/видео.

## Первые решения команды

- [ ] Вписать имена и подтвердить интегратора.
- [ ] Подтвердить сценарные `lead_time_days`, период пересмотра и страховой запас; записать решение в `docs/decisions.md`.
- [ ] Уточнить можно ли показывать/публиковать реальные или производные данные IEK; до этого использовать синтетическую фикстуру.
- [ ] Получить критерии жюри, если есть отдельная рубрика для трека.
