# Старт подготовки данных — akzhan

- Сделано: прочитаны исходное ТЗ партнёра и спецификация проекта; создан отдельный
  worktree `akzhan` и ветка `feat/akzhan/data-skeleton` от `37527b2`.
- Файлы: `backend/importers/` (список источников, команда, инструкция),
  `pyproject.toml` (подключение нового пакета), `docs/tasks.md` (зона ответственности).
- Результат демо: `python -m backend.importers` находит 12/12 исходных файлов;
  при указании отсутствующей папки показывает 0/12 и завершает работу с кодом 1.
  Содержимое Excel пока не читается; проверка сообщает только наличие файлов.
- Проверки: `python scripts/check.py` завершился с кодом 0: Ruff, mypy,
  6 Python-тестов, frontend format/lint/typecheck, 1 frontend-тест и сборка.
  Пять существующих тестов расчёта остаются `xfail`: эти функции ещё не реализованы.
- Особенность Windows: перед полным check локально выполнены `types:generate`
  и Prettier для `main.tsx`, чтобы убрать CRLF. Git не показывает содержательных
  изменений frontend; они не входят в коммит.
- История стартового изменения: [коммиты ветки](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/commits/feat/akzhan/data-skeleton).
- Следующий шаг: карта колонок IEK, затем чтение одного источника после согласования
  входного формата с участником основного backend. Текущая схема требует
  отсутствующие в реальных таблицах поля клиента/цены; это блокирует передачу
  реального набора в расчёт, но не проверку наличия файлов.
