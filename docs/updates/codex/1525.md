# UI — фактический прогресс, 15:25

- Сделано: экран планирования, фильтры и группировка по поставщику, панель факторов, ручная правка выбранного количества, утверждение и CSV. Демо использует синтетический `PlanningResult` v0.1.0; локальное состояние восстанавливается после перезагрузки.
- Коммит: [6159563](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/commit/6159563). Ветка: [`feat/codex/ui`](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/tree/feat/codex/ui).
- Файлы: `frontend/`, корневой `README.md`, `docs/tasks.md`.
- Проверки: `cd frontend && pnpm check` — lint, typecheck, 3 теста и production build прошли. В браузере проверен путь расчёт → разбор → сценарий товара в пути → правка → утверждение → CSV, а также восстановление после перезагрузки.
- Демо: `cd frontend && pnpm dev`, затем `http://127.0.0.1:5174/`. Для `IEK-CAB-001` увеличение товара в пути с 2 до 12 уменьшает предварительную рекомендацию с 35 до 25; выбранное количество меняется независимо.
- Следующий шаг: подключить реальный backend к `src/lib/planningClient.ts` после согласования контрактов; добавить серверное AI-объяснение. Код UI не меняет числовую рекомендацию backend.
- Блокеры: API, импорт Excel и контракт метаданных товара/источника ещё не готовы. Демо-подписи находятся отдельно от `PlanningResult`; реальные данные партнёра не использованы.
