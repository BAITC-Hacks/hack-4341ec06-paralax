# Backend — этап 7, 17:27

- Сделано: отдельная страница «Как получен результат» и точный JSON для любого SKU. Страница соединяет исходные продажи и документы, корректировки, базу, тренд, сезонность, прогноз, запас, остаток, товар в пути, дефицит, правило поставщика, заказ и срочность. Сохранены ссылки на файл, лист и ячейку, исходный коммит и SHA-256 входных файлов.
- Коммит кода и документации: [b8be492](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/commit/b8be492). Ветка: [`feat/codex/backend`](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/tree/feat/codex/backend).
- Файлы: `backend/trace_sku.py`, `backend/trace_page.py`, два тестовых модуля, `docs/trace-sku.md`, `docs/tasks.md`.
- Проверки: 82 теста прошли; согласованность сохранённых этапов и арифметики проверена на всех 3 561 SKU исходного снимка; `git diff --cached --check` прошёл. Трассировка дополнительно отклоняет смешанные хеши, изменённый расчёт, отсутствие исходной строки остатка и непересчитанный статус.
- Демо: локальные HTML и JSON в `.analysis/trace/`: `IEK 010300001_` без актуального остатка остаётся `unavailable`; при учебном вводе `IEK 081100277_` показывает условный заказ 10, `Systeme 300200851_` — заказ 5. Для `Systeme 300200720_` нулевой заказ сохраняет HIGH-риск. Точные команды — в `docs/trace-sku.md`.
- Ограничения: учебные остатки не являются фактическим складом; срок поставки и страховой запас заданы как параметры сценария. Проверка трассировки не перечитывает исходные Excel и не повторяет оценку выбросов и тренда из первичных строк. Смысл минимума отгрузки IEK требует подтверждения. Производные данные партнёра не добавлены в Git; `main` не менялся.
- Следующий шаг: этап 8, dashboard и переход от таблицы к трассировке SKU, только после команды пользователя.
