# Backend + UI — этап 8, 17:44

- Сделано: локальный React-dashboard закупщика для IEK и Systeme Electric поверх результатов этапа 6: сводка, 3 561 SKU, поиск, фильтры, карточка, ручной ввод актуального остатка/товара в пути и серверный пересчёт этапов 5–6. Полная трассировка этапа 7 открывается по любому SKU. Старый UI по синтетическому контракту сохранён по `/demo`.
- Коммит кода и документации: [98ebff7](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/commit/98ebff7). Ветка: [`feat/codex/backend`](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/tree/feat/codex/backend).
- Файлы: `backend/dashboard_server.py`, `backend/test_dashboard_server.py`, `frontend/src/Stage8Dashboard.tsx`, `frontend/src/stage8.css`, `frontend/src/main.tsx`, `frontend/vite.config.ts`, `frontend/README.md`, `docs/tasks.md`.
- Проверки: 87 backend-тестов прошли, `pnpm check` прошёл (lint, типы, 3 frontend-теста, сборка), `git diff --cached --check` прошёл. В браузере проверены данные dashboard, карточка, изменение остатка 9 → 100 → 9 для Systeme `300200851_` (заказ 5 → 0 → 5), фильтр «К заказу» (2 строки) и полная трассировка.
- Демо: [локальный dashboard](http://127.0.0.1:5176/) запущен с `--scenario demo-user-input`: 3 561 SKU, из них 2 положительные рекомендации, 3 HIGH, 3 091 недоступный результат. Точные команды запуска — в `frontend/README.md`.
- Ограничения: только три SKU имеют учебный ручной ввод; эти остатки не являются фактическим складом и не сверяются автоматически. Новый ввод хранится в памяти сервера до перезапуска. Срок поставки и страховой запас заданы параметрами; смысл минимума IEK требует подтверждения. Утверждение/экспорт реального заказа не подключены, автоматическая отправка не предусмотрена. Производные данные партнёра не добавлены в Git; `main` не менялся.
- Следующий шаг: передать пользователю ветку и работающий локальный экран для самостоятельного редактирования. Этап 9 не выполняется.
