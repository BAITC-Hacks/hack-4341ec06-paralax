# Backend — этап 3, 16:16

- Сделано: помесячный прогноз из базового спроса с отдельными робастным трендом и сезонностью SKU, нейтральными fallback, проверкой происхождения входов, трассировкой и локальным графиком. Ветка `feat/codex/backend` (этот коммит).
- Файлы: `backend/forecast_demand.py`, `backend/test_forecast_demand.py`, `docs/forecast-demand.md`, `docs/tasks.md`.
- Проверки: 3 561 реальных SKU, разложение всех числовых прогнозов на компоненты и SHA-256 входов; 19 синтетических тестов этапов 1–3 прошли. HTML/CSV/JSON для трёх реальных SKU.
- Демонстрация: `python backend/forecast_demand.py --sku 010300001_ --sku 010300002_ --sku 010400433_` после запуска этапов 1–2.
- Следующий шаг: этап 4 — необходимый запас и параметры срока поставки/страхового запаса; ждать команды пользователя.
- Ограничения: агрегированная денежная сезонность бренда не используется для количества SKU; прогноз сентября относится ко всему месяцу при неполных наблюдениях; 554 SKU без базового спроса имеют `null` прогноз. Производные данные локальны, `main` не изменён.
