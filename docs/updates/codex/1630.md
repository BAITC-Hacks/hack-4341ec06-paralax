# UI — фактический прогресс, 16:30

- Сделано: в `feat/codex/ui` после `git pull --ff-only` заменён прежний насыщенный демо-интерфейс на минималистичный каркас из обзора, рекомендаций, черновика по поставщикам и источников. Страницы получают типизированный синтетический `PlanningResult`; названия и единицы товара берутся из отдельного демо-каталога. Реальные партнёрские данные и backend не используются.
- Коммит: [c4c886e](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/commit/c4c886e). Ветка: [`feat/codex/ui`](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/tree/feat/codex/ui).
- Файлы: `frontend/src/App.tsx`, `frontend/src/pages/`, `frontend/src/components/RecommendationDetail.tsx`, `frontend/src/lib/presentation.ts`, `frontend/src/styles.css`, `frontend/README.md`, `docs/tasks.md`, `.gitignore`.
- Проверки: `cd frontend && pnpm check` — lint, TypeScript, 3 теста и production build прошли; `git diff --cached --check` прошёл. В локальном браузере проверены четыре раздела, раскрытие рекомендации и адаптивная таблица.
- Демо: `cd frontend && pnpm dev`, затем открыть адрес, напечатанный Vite. Черновик показывает 3 позиции IEK и не отправляет заказ.
- Следующий шаг: согласовать поля метаданных товара и источников в общем контракте, затем подключить адаптер выбранного backend к `PlanningResult` и отдельно реализовать правку, утверждение и экспорт.
- Блокеры: контракт результата пока не содержит название, единицу и происхождение остатка; действия заказа в этом каркасе намеренно отключены.
