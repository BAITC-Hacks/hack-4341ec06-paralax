# Backend — этап 4, 16:38

- Сделано: необходимый запас рассчитан из прогноза за явно заданный срок поставки и прозрачного страхового запаса в днях. Для частичных месяцев сохраняются календарные доли и месячные вклады; неизвестный прогноз остаётся неизвестным. Дефицит и заказ не рассчитываются.
- Коммит: [01c2ba1](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/commit/01c2ba1). Ветка: [`feat/codex/backend`](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/tree/feat/codex/backend).
- Файлы: `backend/target_stock.py`, `backend/test_target_stock.py`, `docs/target-stock.md`, `docs/decisions.md`, `docs/tasks.md`.
- Проверки: 26 синтетических тестов этапов 1–4 прошли. Реальный прогноз обработан для 3 561 SKU; 554 имеют статус `unavailable`. Для каждой рассчитанной строки проверены сумма помесячных вкладов и формула необходимого запаса. `git diff --cached --check` прошёл.
- Демо: `python backend/target_stock.py --planning-date 2026-09-23 --lead-time-days 30 --safety-stock-days 7 --sku 010300001_ --sku 010300002_ --sku 010400433_`. Локальные HTML/CSV/JSON лежат в `.analysis/target-stock/`, например `iek-010300001_.html`; страница проверена в браузере.
- Следующий шаг: только по команде пользователя — этап 5, дефицит с отдельным текущим остатком и товаром в пути.
- Ограничения: в книгах нет договорного lead time; 30 и 7 дней — иллюстративные параметры. Для Systeme Electric нет подтверждённой единой даты снимка. Предполагается равномерный спрос внутри месяца, поскольку дневного прогноза нет. Производные данные партнёра не добавлены в Git; `main` не менялся.
