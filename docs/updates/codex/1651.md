# Backend — этап 5, 16:51

- Сделано: отдельный расчёт `raw_deficit = target_stock − current_stock − goods_in_transit` и `deficit = max(0, raw_deficit)` без округления и заказа. Добавлено извлечение балансов из исходных Excel с сохранением файла, листа, ячейки, партии и качества снимка. Исторический остаток IEK не подставляется как текущий; пустые партии не становятся нулём.
- Коммит: [537d5a8](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/commit/537d5a8). Ветка: [`feat/codex/backend`](https://github.com/BAITC-Hacks/hack-4341ec06-paralax/tree/feat/codex/backend).
- Файлы: `backend/extract_balances.py`, `backend/deficit.py`, их синтетические тесты, `docs/deficit.md`, `docs/decisions.md`, `docs/tasks.md`.
- Проверки: 47 тестов этапов 1–5 прошли. Обработаны 3 561 SKU: 469 условных вычислений (33 с положительным дефицитом, 436 с нулём), 3 092 недоступны, 0 подтверждённых текущих. Для всех числовых результатов отдельно сверены знаковая формула и `max(0, ...)`; `git diff --cached --check` прошёл.
- Демо: `python backend/extract_balances.py`, затем `python backend/deficit.py --sku 010300001_ --sku 300200851_ --sku 300200720_`. Локальные HTML/CSV/JSON находятся в `.analysis/deficit/`; HTML условного расчёта Systeme и недоступного IEK проверены в браузере.
- Следующий шаг: этап 6 (MOQ, округление, срочность и рекомендуемый заказ) только после команды пользователя и подтверждения смысла правил поставщика.
- Ограничения: IEK не имеет текущего датированного остатка; дата/охват сводки Systeme не подтверждены. Результат Systeme условный и не является решением об отправке заказа. Производные данные партнёра не добавлены в Git; `main` не менялся.
