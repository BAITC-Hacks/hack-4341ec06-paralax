> [!NOTE]
> Архив прежнего README, сохранённый при подготовке страницы проекта для жюри.
> Ниже есть исходные требования, планы и устаревшие примеры запуска. Актуальные
> функции и команды приведены в [README проекта](../README.md).

# hack-4341ec06-paralax
Hackathon team repository for Paralax

Рабочий интерфейс менеджера находится в [frontend/](frontend/README.md). Он подключён к локальному FastAPI; запуск и ограничения описаны в инструкции frontend.

# HackAlem AI — Автоматизация формирования заказов поставщикам

> Статус: UI, backend/API и расчёт IEK объединены. Срок поставки и страховой запас остаются сценарными параметрами, охват складов требует подтверждения. OpenAI-объяснения реализованы в расчётном модуле, но HTTP-маршрут пока возвращает резервный текст. Текущие границы MVP зафиксированы в [спецификации](docs/spec.md).
>
> Для начала работы: [правила команды](AGENTS.md) · [задачи и владельцы](docs/tasks.md) · [контракты](contracts/README.md) · [тестовые сценарии](fixtures/scenarios.md).

## Быстрый старт команды

**[Методология расчёта →](docs/methodology.md)** — четыре источника данных, обработка
аномалий и дефицита, пять моделей, выбор по временной валидации, формула заказа,
контрольные примеры, роль OpenAI и измеренные ограничения точности.

Расчётная часть объединена с API: [команды запуска](docs/prediction-run.md),
[карта четырёх входных книг IEK](docs/data-map.md), [методы и официальные источники](docs/prediction-design.md).
Результат — JSON на 50 SKU. OpenAI подключён к объяснениям факторов; числовой
прогноз воспроизводим и работает без API. Ограничения точности и данных указаны
в инструкции запуска.

Полное ТЗ с видом экрана, API, распределением на троих и почасовыми этапами: [docs/spec.md](docs/spec.md). Фактические статусы и владельцы: [docs/tasks.md](docs/tasks.md).

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
cd frontend
npm ci
cd ..
.\.venv\Scripts\python.exe scripts/check.py
```

На Linux/macOS используйте `.venv/bin/python` вместо `.venv\Scripts\python.exe`.
`check` запускает Python format/lint/typecheck/pytest и frontend format/lint/typecheck/test/build. Приёмочные тесты расчёта выполняются без `xfail`. GitHub Actions выполняет те же проверки для push/PR.

Для проверки через ваш OpenAI API задайте ключ в локальном `.env` и выполните:

```powershell
.\.venv\Scripts\python.exe scripts/check.py --live-openai
```

Команда дополнительно выполняет пять реальных платных запросов на синтетических
сценариях. Ошибка API или fallback означает провал проверки. JSON-отчёт сохраняется
в `outputs/mvp-live-check.json`; ключ и партнёрские Excel в API-проверку не попадают.

Для локального просмотра UI запустите backend, затем `npm run dev --prefix frontend` и откройте `http://127.0.0.1:5173`. Исходные 12 Excel-книг IEK и Systeme Electric находятся в `data/` по указанию команды; `data/private/` и `data/raw/` остаются исключёнными. API-ключ в `.env` на серверной стороне; никогда не вводите его во frontend.

Для запуска backend: `.\.venv\Scripts\python.exe -m uvicorn backend.app:app --reload` (Linux/macOS: `.venv/bin/python -m uvicorn backend.app:app --reload`). Документация маршрутов — `http://127.0.0.1:8000/docs` и [contracts/api.md](contracts/api.md). `POST /api/planning-runs` принимает нормализованный JSON и вызывает `backend.planning.plan`; черновик можно править, утверждать и экспортировать. `POST /api/demo/planning-runs` оставлен как статический синтетический мок для UI. Маршрут объяснения пока возвращает резервный текст.

Импорт четырёх книг IEK и локальный расчёт 50 SKU: `.\.venv\Scripts\python.exe -m backend.cli --data-dir data --output outputs/iek-planning-result.json --limit 50`. Затем `outputs/iek-planning-result.input.json` можно отправить в `POST /api/planning-runs` или нажать «IEK · Excel → Рассчитать» в UI. Если подготовленного файла нет, backend импортирует книги по запросу. Подробности и ограничения — в [docs/prediction-run.md](docs/prediction-run.md).

```powershell
$payload = Get-Content outputs/iek-planning-result.input.json -Raw -Encoding UTF8
Invoke-RestMethod -Uri http://127.0.0.1:8000/api/planning-runs -Method Post -ContentType 'application/json; charset=utf-8' -Body $payload
```

Для разработки интерфейса используйте синтетический [мок результата](fixtures/planning-result.demo.json). Готовый типизированный импорт — `planningDemo` из `frontend/src/mocks/planning-demo.generated.ts`, типы — `frontend/src/types/planning.generated.ts`. Источник типов — JSON Schema в `contracts/`; после её изменения выполните `npm run types:generate` в `frontend/`. Команда `check` проверит, что генерация не устарела и мок соответствует схеме.

## О проекте

> [!NOTE]
> Разделы ниже сохраняют исходное описание кейса и целевого продукта, включая ещё
> не реализованные возможности. Фактический алгоритм и команды этой ветки описаны
> в [методологии](docs/methodology.md) и [инструкции запуска](docs/prediction-run.md).

Проект готовится для кейса ТОО «Электрокомплект» в рамках **HackAlem AI**.

Цель — автоматизировать расчёт потребности в пополнении склада и формировать для менеджера отдела закупа рекомендованные заказы поставщикам на основе фактических данных о продажах, остатках, товарах в пути и сроках поставки.

Система не отправляет заказ поставщику автоматически. Финальное решение принимает ответственный сотрудник.

---

## Проблема

Сейчас менеджер отдела закупа рассчитывает потребность в пополнении склада вручную, консолидируя данные в Excel.

Из-за трудоёмкости такого анализа:

- расчёты выполняются нечасто и не в режиме реального времени;
- по части SKU возникают избыточные запасы;
- по другим SKU возникает дефицит;
- разовые крупные продажи искажают регулярный спрос;
- периоды отсутствия товара приводят к недооценке реального спроса;
- компания несёт дополнительные издержки на хранение и теряет продажи из-за stockout.

---

## Ценность решения

Сервис позволяет:

- снизить объём избыточных запасов;
- уменьшить число случаев дефицита;
- сократить упущенные продажи;
- повысить точность закупочных решений;
- учитывать сезонность и устойчивый рост спроса;
- исключать разовые крупные заказы из расчёта регулярной потребности;
- объяснять менеджеру, почему система рекомендует конкретное количество товара.

---

## Пользователь

Основной пользователь — **менеджер отдела закупа**.

### Основной сценарий

1. Менеджер запускает расчёт по складу или категории.
2. Система анализирует входные данные.
3. Для каждого артикула рассчитывается потребность.
4. Система формирует список рекомендованных заказов.
5. Заказы группируются по поставщикам.
6. По каждой позиции показывается краткое обоснование расчёта.
7. Менеджер проверяет рекомендации.
8. При необходимости корректирует количество.
9. Подтверждает заказ.
10. Результат можно экспортировать во внешний формат.

---

## Входные данные

Сервис использует следующие источники.

### История продаж

Минимальные поля:

- дата продажи;
- артикул;
- наименование;
- количество;
- номер документа продажи;
- цена — если доступна;
- склад.

### Остатки

- текущий остаток по каждому артикулу;
- остаток по складам;
- остаток на дату.

### Stockout

Периоды, когда товар отсутствовал на складе.

Эта информация необходима, потому что фактические продажи во время отсутствия товара не отражают реальный спрос.

### Товары в пути

Количество товара, которое уже заказано у поставщика, но ещё не поступило на склад.

### Поставщики

Справочник поставщиков:

- поставщик;
- артикулы;
- срок поставки;
- дополнительные условия поставки — если доступны.

### Дополнительные данные

- категории товаров;
- прогноз прироста спроса;
- материальная ведомость из 1С.

---

# Логика решения

Общая логика расчёта:

```text
История продаж
      +
Текущие остатки
      +
Товары в пути
      +
Stockout
      +
Lead Time поставщика
      +
Категория товара
      +
Прогноз роста
      ↓
Очистка данных
      ↓
Поиск аномальных продаж
      ↓
Оценка регулярного спроса
      ↓
Учёт сезонности
      ↓
Учёт устойчивого роста
      ↓
Компенсация stockout
      ↓
Расчёт потребности
      ↓
Рекомендуемый заказ
      ↓
Группировка по поставщику
      ↓
Dashboard / Export
```

---

# Методология расчёта

## 1. Подготовка данных

Перед прогнозированием данные нормализуются и объединяются по артикулу.

Проверяются:

- пропуски;
- дубликаты;
- отрицательные значения;
- некорректные даты;
- аномальные количества;
- непрерывность временного ряда.

Продажи агрегируются по выбранному временному интервалу:

- день;
- неделя;
- месяц.

Рабочий интервал должен определяться в зависимости от объёма и детализации предоставленных данных.

---

## 2. Выявление разовых крупных заказов

Одна из ключевых задач — не позволить крупной единичной продаже искусственно увеличить прогноз регулярного спроса.

Пример:

```text
Обычные продажи:
95, 102, 110, 98, 105

Разовая продажа:
1000
```

Значение `1000` не должно восприниматься системой как устойчивый рост спроса.

### Учитываемые признаки

При обнаружении аномалий могут использоваться:

- размер заказа относительно исторического распределения;
- отклонение от медианного спроса;
- частота аналогичных объёмов в истории;
- доля одного клиента в продажах артикула;
- повторяемость крупного заказа;
- устойчивость изменения спроса во времени.

### Базовый подход

Для MVP рекомендуется использовать устойчивые статистические методы.

Например:

- **IQR — Interquartile Range**;
- **MAD — Median Absolute Deviation**;
- rolling median / rolling statistics.

Пример логики через IQR:

```text
IQR = Q3 - Q1

нижняя граница = Q1 - k × IQR
верхняя граница = Q3 + k × IQR
```

Продажи значительно выше верхней границы рассматриваются как кандидаты на разовый выброс.

Важно: выброс не должен автоматически удаляться. Он должен быть классифицирован системой как потенциально нерегулярный спрос с учётом клиента и истории товара.

---

## 3. Регулярный спрос

После обработки выбросов строится очищенный временной ряд регулярных продаж.

Пример:

```text
Фактические продажи:
100, 105, 98, 110, 950, 102

Регулярный ряд:
100, 105, 98, 110, 102
```

На его основе рассчитывается базовый спрос.

---

## 4. Сезонность

Алгоритм должен учитывать повторяющиеся сезонные изменения.

Примеры:

- рост спроса летом;
- падение зимой;
- регулярный рост определённых категорий в отдельные месяцы.

Простое среднее значение по всей истории использовать недостаточно, если у товара присутствует выраженная сезонность.

Для MVP могут применяться:

- seasonal moving average;
- Holt-Winters;
- Prophet;
- SARIMA;
- другой алгоритм временных рядов, если он обеспечивает объяснимый результат.

Конкретная модель должна выбираться после анализа структуры предоставленного датасета.

---

## 5. Устойчивый рост спроса

Система должна отличать устойчивый тренд от единичного скачка.

Пример:

```text
100 → 110 → 125 → 140 → 155
```

Такой ряд может свидетельствовать о реальном росте спроса.

В отличие от:

```text
100 → 105 → 900 → 98 → 110
```

где `900` с высокой вероятностью является аномальным событием.

При подтверждённом устойчивом тренде прогноз должен корректироваться в сторону роста.

---

## 6. Компенсация stockout

Если товара не было на складе, продажи в этот период искусственно занижены.

Пример:

```text
Обычный спрос: 10 шт./день

5 дней товар отсутствовал.

Фактические продажи за эти дни:
0
```

Нельзя считать, что спрос был равен нулю.

Сервис должен оценить потенциальный упущенный спрос на основе соседних периодов и исторического поведения товара.

Упрощённая логика:

```text
Estimated Lost Demand =
Expected Demand During Stockout
-
Actual Sales During Stockout
```

После этого прогнозируемый спрос корректируется вверх.

---

# Расчёт рекомендуемого заказа

Концептуально потребность можно представить как:

```text
Recommended Order =
Forecast Demand During Coverage Period
+ Safety Stock
- Current Stock
- Goods In Transit
```

где:

- `Forecast Demand` — прогноз регулярного спроса;
- `Safety Stock` — страховой запас;
- `Current Stock` — текущий остаток;
- `Goods In Transit` — уже заказанный товар;
- `Coverage Period` — период, который должен покрыть новый заказ с учётом срока поставки.

Формула является базовой моделью. Итоговая реализация может корректироваться после анализа реальных данных партнёра.

---

# Срочность заказа

Для каждой позиции система должна определять уровень риска дефицита.

Например:

```text
CRITICAL
HIGH
MEDIUM
LOW
```

В расчёте могут учитываться:

- текущий остаток;
- средний ежедневный спрос;
- прогноз спроса;
- товары в пути;
- срок поставки;
- вероятность stockout до следующей поставки.

---

# Выход системы

Результат отображается в таблице или dashboard.

Пример:

| Артикул | Поставщик | Остаток | В пути | Прогноз | Рекомендуемый заказ | Срочность | Обоснование |
|---|---|---:|---:|---:|---:|---|---|
| SKU-001 | Supplier A | 120 | 50 | 300 | 180 | HIGH | Остатка недостаточно на период поставки |
| SKU-002 | Supplier B | 700 | 0 | 200 | 0 | LOW | Текущего запаса достаточно |
| SKU-003 | Supplier A | 40 | 100 | 280 | 190 | CRITICAL | Высокий риск stockout |

---

# Explainability

Каждая рекомендация должна иметь объяснение.

Пример:

```text
Рекомендуется заказать: 420 шт.

Причины:
- прогноз спроса на следующий период: 500 шт.;
- текущий остаток: 130 шт.;
- в пути: 100 шт.;
- ожидаемый stockout через 12 дней;
- lead time поставщика: 25 дней;
- обнаружена разовая продажа 800 шт., исключённая из регулярного спроса;
- наблюдается устойчивый рост спроса +8%.
```

Менеджер должен понимать, почему алгоритм получил именно такой результат.

---

# Группировка по поставщикам

Финальный список заказов группируется по поставщику.

Пример:

```text
Supplier A
├── SKU-001 — 180 шт.
├── SKU-003 — 190 шт.
└── SKU-010 — 80 шт.

Supplier B
├── SKU-021 — 500 шт.
└── SKU-032 — 150 шт.
```

Это позволяет сформировать готовый проект заказа для конкретного поставщика.

---

# Dashboard

Минимальный интерфейс должен содержать:

### Сводка

- количество анализируемых SKU;
- позиции с высоким риском stockout;
- позиции с избыточным запасом;
- количество рекомендуемых заказов;
- количество обнаруженных аномальных продаж.

### Таблица рекомендаций

Фильтры:

- поставщик;
- категория;
- склад;
- срочность;
- артикул.

### Карточка товара

Для выбранного артикула:

- история продаж;
- очищенный спрос;
- аномальные продажи;
- прогноз;
- текущий остаток;
- товары в пути;
- периоды stockout;
- рекомендуемый заказ;
- объяснение расчёта.

---

# Экспорт

Результаты должны иметь возможность экспорта.

Минимально:

- CSV;
- XLSX.

Выходной формат должен быть совместим с текущей учётной системой компании.

---

# Ограничения

## Запрещено

- автоматически отправлять заказ поставщику без подтверждения ответственного сотрудника;
- использовать неанонимизированные данные клиентов.

## Обязательно

- приватность данных;
- объяснимость решений;
- устойчивость к выбросам;
- корректная работа с аномальными продажами;
- учёт stockout;
- совместимость выходного формата с текущими процессами компании.

---

# Must Have

Для MVP обязательны следующие функции:

- [ ] расчёт потребности по каждому артикулу;
- [ ] использование истории продаж;
- [ ] использование текущих остатков;
- [ ] учёт товаров в пути;
- [ ] учёт категории товара;
- [ ] учёт прогноза прироста;
- [ ] учёт сезонности;
- [ ] определение устойчивого роста спроса;
- [ ] оценка упущенного спроса во время stockout;
- [ ] обнаружение разовых крупных заказов;
- [ ] анализ крупных продаж одному клиенту;
- [ ] исключение нерегулярных выбросов из регулярного прогноза;
- [ ] рекомендуемое количество заказа;
- [ ] поставщик;
- [ ] срочность;
- [ ] объяснение рекомендации;
- [ ] группировка заказов по поставщикам;
- [ ] таблица / dashboard;
- [ ] экспорт результата.

---

# Optional

При наличии времени:

- [ ] приоритизация SKU по риску дефицита;
- [ ] минимальная партия заказа;
- [ ] индивидуальные условия поставщика;
- [ ] визуализация трендов по категориям;
- [ ] подготовка черновика заказа поставщику;
- [ ] автоматическая рассылка только после подтверждения менеджером.

---

# Предлагаемая архитектура

```text
                     ┌──────────────────┐
                     │   Data Sources   │
                     │ CSV / XLSX / 1C │
                     └────────┬─────────┘
                              │
                              ▼
                     ┌──────────────────┐
                     │ Data Processing  │
                     │ validation       │
                     │ aggregation      │
                     │ normalization    │
                     └────────┬─────────┘
                              │
                 ┌────────────┴────────────┐
                 ▼                         ▼
        ┌────────────────┐        ┌────────────────┐
        │ Outlier Engine │        │ Stockout Engine│
        │ one-off orders │        │ lost demand    │
        └───────┬────────┘        └───────┬────────┘
                │                         │
                └────────────┬────────────┘
                             ▼
                  ┌─────────────────────┐
                  │ Forecasting Engine  │
                  │ trend / seasonality │
                  └──────────┬──────────┘
                             ▼
                  ┌─────────────────────┐
                  │ Replenishment Engine│
                  │ order calculation   │
                  └──────────┬──────────┘
                             ▼
                  ┌─────────────────────┐
                  │ Explainability      │
                  │ reasons / factors   │
                  └──────────┬──────────┘
                             ▼
                  ┌─────────────────────┐
                  │ API / Dashboard     │
                  └─────────────────────┘
```

---

# Рекомендуемая структура проекта

```text
.
├── README.md
├── backend/
│   ├── api/
│   ├── services/
│   │   ├── forecasting/
│   │   ├── outliers/
│   │   ├── stockout/
│   │   └── replenishment/
│   ├── models/
│   ├── schemas/
│   └── main.py
│
├── frontend/
│   └── ...
│
├── data/
│   ├── raw/
│   ├── processed/
│   └── synthetic/
│
├── notebooks/
│   └── exploration.ipynb
│
├── tests/
│   ├── test_forecast.py
│   ├── test_outliers.py
│   ├── test_stockout.py
│   └── test_replenishment.py
│
└── docs/
    └── methodology.md
```

Структура является рекомендуемой и может быть адаптирована под выбранный стек.

---

# Проверка решения

## Test 1 — изменение товаров в пути

Если увеличить количество товара в пути, рекомендуемый новый заказ должен уменьшиться.

```text
goods_in_transit ↑
→
recommended_order ↓
```

---

## Test 2 — сезонность

Для товара с выраженной сезонностью прогноз должен повторять сезонный паттерн, а не рассчитываться только через среднее значение всей истории.

---

## Test 3 — stockout

Если добавить период отсутствия товара, система должна оценить упущенный спрос.

```text
raw sales forecast
<
stockout-adjusted forecast
```

---

## Test 4 — разовый крупный заказ

Если искусственно добавить очень крупную единичную продажу:

```text
100
110
95
105
1000
```

рекомендуемое регулярное количество не должно резко увеличиться только из-за значения `1000`.

---

## Test 5 — поставщики

Финальный список должен группироваться по поставщикам.

Каждая строка должна содержать:

- артикул;
- поставщика;
- рекомендуемое количество;
- срочность;
- объяснение.

---

# Данные для разработки

Партнёр предоставляет:

- выгрузку истории продаж;
- остатки;
- отчёт V2;
- данные по динамике продаж.

До получения полного датасета допускается использование **синтетических данных с реалистичным распределением**.

Данные клиентов должны быть обезличены.

---

# Запуск

> Команды необходимо актуализировать после выбора окончательного технологического стека.

Пример общего сценария:

```bash
git clone <repository-url>
cd <repository>

# Backend
cd backend
pip install -r requirements.txt
python main.py
```

Frontend, database и дополнительные сервисы запускаются в соответствии с выбранной архитектурой проекта.

---

# Definition of Done

MVP считается готовым, если:

1. Система принимает тестовый набор данных.
2. Для каждого артикула рассчитывается прогноз спроса.
3. Учитываются остатки и товары в пути.
4. Учитывается сезонность.
5. Учитывается устойчивый рост.
6. Компенсируются периоды stockout.
7. Разовые крупные продажи обнаруживаются и не искажают регулярный прогноз.
8. Формируется рекомендуемое количество заказа.
9. Заказы группируются по поставщикам.
10. Каждая рекомендация содержит объяснение.
11. Результат доступен через таблицу/dashboard.
12. Результат можно экспортировать.
13. Заказ не отправляется поставщику автоматически без подтверждения менеджера.

---

## Владелец кейса

**ТОО «Электрокомплект»**
https://ekt.kz

## Хакатон

**HackAlem AI**
